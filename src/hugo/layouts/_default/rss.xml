{{- $pctx := . -}}
{{- if .IsHome -}}{{ $pctx = .Site }}{{- end -}}
{{- $pages := slice -}}
{{- if or $.IsHome $.IsSection -}}
{{- $pages = where $pctx.RegularPages ".Params.layout" "!=" "staticpage" -}}
{{- else -}}
{{- $pages = $pctx.Pages -}}
{{- end -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
{{- $pages = $pages | first $limit -}}
{{- end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ if eq  .Title  .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{.}} on {{ end }}{{ .Site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne  .Title  .Site.Title }}{{ with .Title }}in {{.}} {{ end }}{{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo -- gohugo.io</generator>{{ with .Site.LanguageCode }}
	<language>{{.}}</language>{{end}}{{ if (and .Site.Author.email .Site.Author.name)}}
	<managingEditor>{{ $.Site.Author.name }} &lt;{{$.Site.Author.email}}&gt;</managingEditor>
	<webMaster>{{ $.Site.Author.name }} &lt;{{$.Site.Author.email}}&gt;</webMaster>
	<copyright>{{ $.Site.Author.name }} &lt;{{$.Site.Author.email}}&gt;</copyright>
	{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{- with .OutputFormats.Get "RSS" -}}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end -}}
    {{ range $pages }}
	<item>
		<title>{{if .Params.rsstitle}} {{.Params.rsstitle}} {{else}} {{ .Title }} {{end}}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
	  {{ with .Site.Author.email }}<author>{{ with $.Site.Author.name }}{{.}} {{end}}&lt;{{.}}&gt;</author>{{end}}
      <guid>{{ .Permalink }}</guid>
      <description>{{ .Description | html }}</description>
    </item>
    {{ end }}
  </channel>
</rss>
